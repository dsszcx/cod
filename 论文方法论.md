# 大语言模型先验引导的伪装目标检测

## 3. Methodology（方法论）

---

### 3.1 总体框架 Overview

本文提出一种**大语言模型先验引导的伪装目标检测网络（LLM-Prior-Guided COD Network, LPGNet）**。整体流程分为三个阶段：

1. **先验生成**：大语言模型 Qwen2.5-VL-3B 对输入图像进行语义理解，生成语义标签与目标位置；
2. **先验编码**：将文本形式的标签与位置编码为可学习的空间先验张量；
3. **先验引导分割**：先验张量与原始图像拼接为多通道输入，送入 PVTv2-B2 主干与多尺度解码器，完成伪装目标分割。

**总体框架图：**

```
输入图像 I ∈ R^{H×W×3}
     │
     ├──────────────────────────────────────┐
     ▼                                      ▼
  Qwen2.5-VL-3B (LLM)                 PVTv2-B2 Backbone
  ├─ tags T = {t₁,…,t_K}            (输入 5 通道 X_in)
  └─ bbox B = (x₁,y₁,x₂,y₂)            │ F₂, F₃, F₄
     │                                 ▼
     ▼                          RFB_modified (多尺度空洞卷积)
  Prior Encoder                      │ F₂ʳᶠᵇ, F₃ʳᶠᵇ, F₄ʳᶠᵇ
  ├─ P_pos: 位置高斯热力图             ▼
  └─ S_sem: 语义响应图        Neighbor Connection Decoder (NCD)
     │                                 │ S_g
     ▼                                 ▼
  5 通道输入 X_in = [I; P_pos; S_sem]   Reverse Stage (GRA ×3)
                                       │ S₅, S₄, S₃
                                       ▼
                                  分割结果 {S_g, S₅, S₄, S₃}
```

---

### 3.2 符号定义 Notation

| 符号 | 含义 |
|:---|:---|
| $I \in \mathbb{R}^{H \times W \times 3}$ | 输入 RGB 图像 |
| $T = \{t_1, \dots, t_K\}$ | LLM 生成的 $K$ 个语义标签 |
| $B = (x_1, y_1, x_2, y_2)$ | LLM 生成的目标包围框 |
| $c \in [0,1]$ | LLM 输出的置信度 |
| $P_{pos} \in \mathbb{R}^{1 \times H \times W}$ | 位置先验图（高斯热力图） |
| $\mathbf{b} \in \mathbb{R}^{V}$ | 语义词袋向量（$V$ 为词表大小） |
| $S_{sem} \in \mathbb{R}^{1 \times H \times W}$ | 语义响应图 |
| $X_{in} \in \mathbb{R}^{5 \times H \times W}$ | 网络 5 通道输入 |
| $F_2, F_3, F_4$ | PVTv2 第 3/4/5 阶段特征 |
| $\{S_g, S_5, S_4, S_3\}$ | 网络四个尺度的显著性图 |
| $\hat{Y}$ | 真实掩码 (Ground Truth) |

---

### 3.3 大语言模型先验生成 LLM Prior Generation

给定输入图像 $I$，我们使用指令微调的视觉-语言模型 Qwen2.5-VL-3B $\mathcal{F}_{\theta}$ 生成结构化先验：

$$\{T, B, c\} = \mathcal{F}_{\theta}(I \mid \mathcal{P})$$

其中 $\mathcal{P}$ 为设计的提示模板，要求模型仅输出 JSON 格式：

$$\mathcal{P}: \quad \{\text{"tags"}: [t_1,\dots,t_K], \; \text{"bbox"}: [x_1,y_1,x_2,y_2], \; \text{"confidence"}: c \}$$

**离线批量标注**：训练阶段对全部训练图像离线执行上式并保存为标注库 $\mathcal{A} = \{(I_n, T_n, B_n, c_n)\}_{n=1}^{N}$，避免训练过程中的 LLM 推理开销。

---

### 3.4 先验编码 Prior Encoding

#### 3.4.1 位置先验图（高斯热力图）

将包围框 $B$ 转换为二维高斯热力图 $P_{pos}$。设包围框中心与尺寸为：

$$(c_x, c_y) = \left( \frac{x_1+x_2}{2},\; \frac{y_1+y_2}{2} \right), \quad (w, h) = (x_2-x_1,\; y_2-y_1)$$

则热力图在像素 $(i,j)$ 处的值为：

$$P_{pos}(i,j) = \exp\left( -\frac{(i-c_x)^2}{2\sigma_x^2} - \frac{(j-c_y)^2}{2\sigma_y^2} \right)$$

其中标准差与目标尺寸成正比：

$$\sigma_x = \max(w \cdot \alpha, \epsilon), \quad \sigma_y = \max(h \cdot \alpha, \epsilon), \quad \alpha = 0.15$$

$\alpha$ 控制先验的"聚焦强度"，$\epsilon$ 防止除零。当 LLM 未检测到目标（$c=0$）时，$P_{pos}$ 置为零图，网络退化为无先验模式。

#### 3.4.2 语义词袋向量

对标签集合 $T$，构建词袋表示：

$$\mathbf{b}_n(v) = \frac{\sum_{t \in T_n} \mathbb{1}[t = v]}{\sum_{v'=1}^{V} \sum_{t \in T_n} \mathbb{1}[t = v'] + \epsilon}$$

即每个标签在其出现次数上归一化，$V$ 为从训练集统计得到的词表大小，$\mathbb{1}[\cdot]$ 为指示函数。

#### 3.4.3 语义响应图

词袋向量通过语义投影器 $\Phi$ 映射为标量语义强度，并广播为空间图：

$$s = \sigma\Big( \mathbf{W}_2 \cdot \text{GELU}(\mathbf{W}_1 \mathbf{b}) \Big) \in \mathbb{R}$$

$$S_{sem}(i,j) = s, \quad \forall (i,j)$$

其中 $\mathbf{W}_1 \in \mathbb{R}^{d \times V}$，$\mathbf{W}_2 \in \mathbb{R}^{1 \times d}$ 为可学习参数，$\sigma$ 为 Sigmoid 函数。

---

### 3.5 先验引导分割网络 Prior-Guided Network

#### 3.5.1 多通道输入融合

将归一化图像 $\bar{I}$、位置先验图与语义响应图沿通道拼接：

$$X_{in} = \text{Concat}\big( \bar{I},\; P_{pos},\; S_{sem} \big) \in \mathbb{R}^{5 \times H \times W}$$

#### 3.5.2 PVTv2 主干特征提取

采用 Pyramid Vision Transformer V2 (PVTv2-B2) 作为主干。第一层 Patch Embedding 的输入通道扩展为 5，预训练权重的前 3 通道直接继承 ImageNet 权重，新增 2 通道用 Kaiming 初始化。得到三级特征：

$$F_2, F_3, F_4 = \text{PVTv2}(X_{in})$$

其中 $F_k \in \mathbb{R}^{C_k \times \frac{H}{2^{k}} \times \frac{W}{2^{k}}}$，$C_2{=}128, C_3{=}320, C_4{=}512$。

#### 3.5.3 RFB 多尺度增强模块

每个特征经 RFB（Receptive Field Block）增强，其使用四种不同膨胀率 $r \in \{1,3,5,7\}$ 的空洞卷积分支：

$$F_k^{rfb} = \text{CBAM}\Big( \text{Conv}_{1\times1}\big( \text{Concat}[ \text{Conv}_1^{r}(F_k) ]_{r} \big) \Big) + F_k$$

其中 CBAM 为通道-空间双注意力模块，末项为残差连接。

#### 3.5.4 邻接连接解码器 NCD

采用邻接连接解码器（Neighbor Connection Decoder）逐级融合深层语义与浅层细节：

$$x_1 = F_4^{rfb}, \quad x_2 = \text{Up}(x_1) \odot F_3^{rfb}, \quad x_3 = \text{Up}(x_2) \odot \text{Up}(x_1) \odot F_2^{rfb}$$

$$S_g = \text{Conv}_{1\times1}(\text{CBAM}(\text{Conv}(x_3)))$$

其中 $\odot$ 为逐元素相乘（门控），$\text{Up}$ 为双线性上采样。

#### 3.5.5 反向引导细化 Reverse Stage

借鉴 SINet-V2 的 GRA（Group Reversal Attention）实现从粗到细的渐进细化。定义反向注意力：

$$Y = 1 - \sigma(S)$$

突出"最不确定区域"，经三级 GRA 迭代：

$$X, Y \leftarrow \text{GRA}_w(X, Y), \quad X, Y \leftarrow \text{GRA}_m(X, Y), \quad \_, Y \leftarrow \text{GRA}_s(X, Y)$$

GRA 的核心操作为分组交错拼接与门控更新：

$$X = X + \text{Conv}\big( \text{Interleave}(X, Y) \big), \quad Y = Y + \text{Score}(X)$$

逐级上采样得到 $S_5, S_4, S_3$。

---

### 3.6 混合损失函数 Hybrid E-Loss

采用加权 BCE + E-loss + 加权 IoU 的混合损失，并对四个输出进行深度监督：

$$\mathcal{L} = \mathcal{L}_{S_g} + \mathcal{L}_{S_5} + \mathcal{L}_{S_4} + \mathcal{L}_{S_3}$$

其中每项由三部分构成：

**① 加权二元交叉熵 (WBCE)**：定义自适应权重掩码 $W$：

$$W = 1 + 5 \cdot \big| \text{AvgPool}_{31}(\hat{Y}) - \hat{Y} \big|$$

该权重在目标边缘区域赋予更大权重，迫使模型关注难分像素：

$$\mathcal{L}_{wbce} = \frac{\sum_{i,j} W_{i,j} \cdot \text{BCE}(S_{i,j}, \hat{Y}_{i,j})}{\sum_{i,j} W_{i,j}}$$

**② E-loss（增强对齐度量）**：基于协方差的对齐度量：

$$\phi_{S} = S - \bar{S}, \quad \phi_{Y} = \hat{Y} - \bar{\hat{Y}}$$

$$E_{FM} = \frac{2\phi_S \phi_Y}{\phi_S^2 + \phi_Y^2}, \quad Q_{FM} = \frac{(1+E_{FM})^2}{4}$$

$$\mathcal{L}_e = 1 - \text{mean}(Q_{FM})$$

**③ 加权 IoU (WIoU)**：

$$\mathcal{L}_{wiou} = 1 - \frac{\sum (S \cdot \hat{Y} \cdot W) + 1}{\sum (S + \hat{Y}) \cdot W - \sum (S \cdot \hat{Y} \cdot W) + 1}$$

最终每级损失为三者之和：

$$\mathcal{L}_{S_i} = \mathcal{L}_{wbce}(S_i) + \mathcal{L}_e(S_i) + \mathcal{L}_{wiou}(S_i)$$

---

### 3.7 训练与推理流程

**训练**：AdamW 优化器，初始学习率 $10^{-5}$，余弦退火调度，多尺度训练（$r \in \{0.75, 1.0\}$），梯度裁剪阈值 0.5。

**推理**：

```
输入 I → Qwen2.5-VL-3B → {T, B, c}
            → Prior Encoder → {P_pos, b}
            → 5通道输入 X_in → 网络 → S_3 → 后处理(上采样+sigmoid+归一化) → 分割图
```

---

### 3.8 算法伪代码

```
Algorithm 1: LPGNet 训练流程
Input: 训练集 {I_n, Y_n}，LLM 标注库 {T_n, B_n, c_n}，词表 V
Output: 网络参数 θ

1: 统计词表 V ← 所有训练标签
2: 初始化网络 θ ← PVTv2-B2 ImageNet 预训练(3通道) + 新通道Kaiming初始化
3: for epoch = 1 to E do
4:   for each batch {(I, Y)} do
5:     T, B, c ← 从标注库读取（离线）
6:     P_pos ← BBoxToHeatmap(B)          // 式(3)
7:     b ← TagsToBOW(T)                  // 式(4)
8:     s ← Φ(b)                          // 式(5)
9:     X_in ← Concat[Ī, P_pos, s·1]       // 式(6)
10:    {S_g, S_5, S_4, S_3} ← Net(X_in; θ)
11:    L ← Σᵢ [L_wbce(S_i)+L_e(S_i)+L_wiou(S_i)]   // 式(8-13)
12:    θ ← θ − AdamW(L, θ)               // 反向传播
13:   end for
14: end for
15: return θ
```

---

> 本文档为论文方法论草稿，公式编号可在正式撰写时统一调整。