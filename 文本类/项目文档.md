# 📘 项目文档 — SAM3 先验引导伪装目标检测

> 更新日期：2026-09-24

---

## 一、项目概述

**伪装目标检测（Camouflaged Object Detection, COD）**：输入图片，输出**像素级分割掩码**（白=伪装目标，黑=背景）。

### 核心创新

用大模型（Qwen2.5-VL-3B）和 SAM3 生成"目标区域+辅助区域"先验，引导分割网络，显著提升伪装目标分割精度。

**核心成果**：MAE 0.0673（基础模型 0.18，提升 63%）。

### 网络结构

- 骨干：PVTv2-B2（ImageNet 预训练）
- 8通道输入：原图(3ch) + 目标先验(1ch) + 辅助先验(3ch) + 负样本标志(1ch)
- 损失函数：Hybrid E-loss
- 推理输出：1ch 二值分割图

---

## 二、完整 Pipeline

```
Step 1: Qwen 预筛选标注        Step 2: SAM3 区域生成          Step 3: Qwen 判别
 图片→OpenCV候选框→Qwen命名      tag→候选区域masks              选出目标+辅助
 │ (llm_annotator.py)           │ (sam3_regions.py)            │ (region_selector.py)
 ▼                               ▼                              ▼
 prior_annotations.json         sam3_regions/*.npz             prior_sam3/*.npz
                                                               │
                                                               ▼
                                                        Step 4: 训练 (MyTrain_SAM3.py)
                                                        8通道网络（原图+目标+辅助×3+neg_flag）
                                                               │
                                                               ▼
                                                        Step 5: 推理 (MyTest_SAM3.py)
                                                        results_sam3/预测图
```

### 环境分工

| Step | 运行位置 | 环境 | 关键依赖 |
|:---|:---|:---|:---|
| Step 1 (Qwen 标注) | 服务器 cx2 | Python 3.12 | transformers, qwen-vl-utils, OpenCV |
| Step 2 (SAM3 区域) | 服务器 cx1 | Python 3.12 | torch cu128, SAM3 |
| Step 3 (Qwen 判别) | 服务器 cx2 | Python 3.12 | transformers, OpenCV |
| Step 4 (训练) | 本地 RTX 4070 | Python 3.9 | torch cu118 |
| Step 5 (推理) | 本地 RTX 4070 | Python 3.9 | torch cu118 |

---

## 三、文件清单

### 3.1 服务器端（大模型相关）

| 文件 | 作用 |
|:---|:---|
| `llm_annotator.py` | Step1：Qwen 标注生成语义标签。支持 prescreen 模式（OpenCV 检测候选 + Qwen 命名）和 legacy 模式（Qwen 开放问答） |
| `sam3_regions.py` | Step2：读入 Qwen 标签，用 SAM3 文本 prompt 分割生成候选区域 masks |
| `region_selector.py` | Step3：从 SAM3 候选区域中选出目标 + 辅助区域。支持 qwen 模式（视觉提示）和 heuristic 模式（本地启发式） |

### 3.2 本地端（训练/推理）

| 文件 | 作用 |
|:---|:---|
| `Network_PVTv2.py` | 基础网络：PVTv2-B2 骨干 + 简单解码头（单输入） |
| `Network_SAM3.py` | SAM3 网络：PVTv2-B2 骨干 + 8通道输入 + CBAM 双解码头 |
| `MyTrain.py` | 基础版训练脚本（单通道） |
| `MyTrain_SAM3.py` | SAM3 版训练脚本（8通道，支持 CAMO/COD10K 双测试集） |
| `MyTest.py` | 基础版推理 |
| `MyTest_SAM3.py` | SAM3 版推理（支持多数据集、MAE/IoU 指标、可视化） |
| `dataloader.py` | 基础版数据加载 |
| `dataloader_sam3.py` | SAM3 版数据加载（8通道 + 先验同步增强 + 按 basename 配对） |
| `prepare_datasets.py` | 数据集整理（支持 `--full` 全量 COD10K） |

### 3.3 Web 服务

| 文件 | 作用 |
|:---|:---|
| `web_server.py` | Flask Web 界面（基础模型） |
| `web_server_sam3.py` | Flask Web 界面（SAM3 模型） |
| `templates/index.html` | Web 前端页面 |
| `GUI.py` | 桌面 GUI |

### 3.4 工具/辅助

| 文件 | 作用 |
|:---|:---|
| `lib/pvtv2.py` | PVTv2-B2 骨干网络实现 |
| `utils.py` | 训练工具（损失函数、学习率调度等） |
| `verify_pipeline.py` | Pipeline 端到端验证脚本 |
| `新metric.py` | 评估指标计算 |

---

## 四、运行命令

### Step 1：Qwen 预筛选标注（服务器 cx2）

```bash
# prescreen 模式（推荐，适合 3B 小模型）—— OpenCV 检测候选 + Qwen 命名
python llm_annotator.py --data_root ./Imgs/ --split train --mode prescreen \
    --model /home/16t/cr/sr/chatsr/Qwen2.5-VL-3B-Instruct/ \
    --out outputs/prior_annotations.json

# legacy 模式（原始开放式问答）
python llm_annotator.py --data_root ./Imgs/ --split train --mode legacy \
    --model /home/16t/cr/sr/chatsr/Qwen2.5-VL-3B-Instruct/ \
    --out outputs/prior_annotations.json

# 多 GPU 并行
python llm_annotator.py --data_root ./Imgs/ --split train --gpus 0,1,2 \
    --out outputs/prior_annotations.json

# 只处理特定图片列表（如 COD10K CAM 子集）
python llm_annotator.py --data_root ./COD10K-v2/COD10K-v2/Train/Image/ \
    --split train --filelist ./COD10K-v2/COD10K-v2/Info/CAM_train.txt \
    --out outputs/prior_annotations.json
```

**关键参数：**

| 参数 | 说明 | 默认值 |
|:---|:---|:---|
| `--mode` | `prescreen`（推荐）或 `legacy` | `prescreen` |
| `--gpus` | 多 GPU，逗号分隔 | `0` |
| `--num_runs` | 多轮标注投票次数 | `1` |
| `--filelist` | 只处理列表内的文件名 | 无（处理全部） |
| `--max_images` | 限制图片数（调试用） | `-1`（全部） |

**产出**：`outputs/prior_annotations.json`（格式见下方数据格式节）

### Step 2：SAM3 区域生成（服务器 cx1）

```bash
# box-prompt 模式（推荐，使用 LLM 标注的 bbox 作为框提示）
python sam3_regions.py --data_root ./Imgs/ \
    --ann outputs/prior_annotations.json --split train \
    --ckpt ./sam3-main/sam3-main/checkpoints/sam3.pt --out ./sam3_regions/ \
    --prompt_mode box

# text-prompt 模式（原始行为，使用 tag 文本提示）
python sam3_regions.py --data_root ./Imgs/ \
    --ann outputs/prior_annotations.json --split train \
    --ckpt ./sam3-main/sam3-main/checkpoints/sam3.pt --out ./sam3_regions/ \
    --prompt_mode text

# hybrid 模式（box 优先 + text 补充）
python sam3_regions.py --data_root ./Imgs/ \
    --ann outputs/prior_annotations.json --split train \
    --ckpt ./sam3-main/sam3-main/checkpoints/sam3.pt --out ./sam3_regions/ \
    --prompt_mode hybrid
```

**关键参数：**

| 参数 | 说明 | 默认值 |
|:---|:---|:---|
| `--prompt_mode` | `box`（框提示，推荐）/ `text`（文本提示，原始）/ `hybrid`（框+文本） | `box` |

**产出**：`sam3_regions/{split}_{image_stem}.npz`（key: tags, masks, scores, bbox）

### Step 3：Qwen 判别目标/辅助区域（服务器 cx2）

```bash
python region_selector.py --regions ./sam3_regions/ --out ./prior_sam3/ \
    --split train --mode qwen --image_root ./Imgs/ \
    --qwen_model /home/16t/cr/sr/chatsr/Qwen2.5-VL-3B-Instruct/
```

**关键参数：**

| 参数 | 说明 | 默认值 |
|:---|:---|:---|
| `--mode` | `qwen`（Qwen视觉判别）或 `heuristic`（本地启发式） | `heuristic` |
| `--min_target_ratio` | 目标最小面积比（低于此→零先验） | `0.005` |
| `--conf_thresh` | SAM3 最低置信度（低于此→零先验） | `0.6` |
| `--max_aux` | 最大辅助区域数 | `3` |
| `--draw_dir` | 保存候选标注图目录（便于抽查） | 无（临时目录） |

**产出**：`prior_sam3/{split}_{image_stem}.npz`（key: target_mask, aux_masks, target_tag, bbox）

### Step 4：训练（本地 RTX 4070）

```bash
# 单测试集（CAMO）
python MyTrain_SAM3.py --epoch 50 --batchsize 6

# 双测试集（CAMO + COD10K）
python MyTrain_SAM3.py --epoch 50 --batchsize 6 --test_datasets CAMO,COD10K
```

**产出**：`model_pth/SAM3/` 下的 `.pth` 权重文件

### Step 5：推理（本地 RTX 4070）

```bash
python MyTest_SAM3.py --pth_path ./model_pth/SAM3/KKK_best_41.pth
# 预测图存 results_sam3/<权重名>/<数据集>/
```

---

## 五、数据集格式

### 目录结构

```
Dataset/
├── TrainValDataset/
│   ├── Imgs/                   # 训练图片（CAMO 1000 + COD10K 6000）
│   └── GT/                     # 对应 GT 掩码
├── TestDataset/
│   ├── CAMO/                   # CAMO 测试集（250 图 + GT）
│   └── COD10K/                 # COD10K 测试集（4000 图 + GT）
```

### 数据整理

```bash
python prepare_datasets.py            # CAM-only：训练 4038 / 测试 2026
python prepare_datasets.py --full     # 全量：训练 7000 / 测试 4000（推荐）
```

### 先验 JSON 格式（prior_annotations.json）

```json
{
  "train": {
    "image_name.jpg": {
      "tags": ["white bird", "snow", "branch"],
      "bbox": [120, 80, 50, 40],
      "confidence": 0.85
    }
  }
}
```

### 先验 NPZ 格式（prior_sam3/）

| Key | Shape | 说明 |
|:---|:---|:---|
| `target_mask` | `(1, H, W)` | 目标区域二值掩码 |
| `aux_masks` | `(max_aux, H, W)` | 辅助区域掩码（硬负样本） |
| `target_tag` | `(1,)` object | 目标语义标签 |
| `bbox` | `(4,)` | 边界框坐标 |

---

## 六、关键配置

### 训练超参数

| 参数 | 值 |
|:---|:---|
| 图像尺寸 | 352×352 |
| Epochs | 50 |
| Batch size | 6-8 |
| 骨干 | PVTv2-B2 |
| 损失函数 | Hybrid E-loss |
| 最佳模型 | MAE 0.0673 @ Epoch 41 |

### SAM3 模型权重

- 服务器路径：`./sam3-main/sam3-main/checkpoints/sam3.pt`

### Qwen 模型

- 服务器路径：`/home/16t/cr/sr/chatsr/Qwen2.5-VL-3B-Instruct/`
- 本地（Qwen2.5-VL-72B-GGUF）：`./models/Qwen2.5-VL-72B-Instruct-abliterated-Q4_K_M.gguf`

### 骨干预训练权重

- `pretrained/pvt_v2_b2.pth`（101MB，ImageNet 预训练）

### 百度网盘（原始训练权重）

- 链接：https://pan.baidu.com/s/1nz_E-tiGLIE8u1NN3rixJA
- 提取码：`mgc1`

---

## 七、效果总结

### CAMO 测试集（250 张）

| 方案 | MAE ↓ | 提升 |
|:---|:---:|:---:|
| 基础模型（无先验） | ~0.18 | — |
| **SAM3 先验模型** | **0.0673** | **↓ 63%** |

### Qwen 标注质量评估

| 图像类别 | 有效标签率 | 说明 |
|:---|:---:|:---|
| 有明显动物的图 | 85% | 能识别 bird, cat, dog 等 |
| 纯伪装图 | 40-50% | 倾向输出场景描述而非物体名称 |
| 纯背景图 | 0-20% | 大量"无人目标"标记，或幻觉输出 |

---

## 八、已知问题与优化方向

### 已修复问题（2026-09-24）

**Step 3 Qwen 视觉提示 bug（已修复）**

Qwen 模式在 Step 3 中几乎全部 fallback 到 heuristic，根本原因有 3 个：

| 问题 | 根因 | 修复方案 |
|:---|:---|:---|
| System prompt 冲突 | `generate_with_text` 硬编码 `"Output only JSON"`，与 "只输出数字" 指令矛盾 | 增加 `system_prompt` 参数，Step 3 传入专用 prompt |
| 视觉提示太弱 | `draw_candidates` 只画 2px 细轮廓 + 小号文字，Qwen 3B 看不清 | 改为 3px 粗矩形框 + 大号标签 + 填充背景 |
| n=1 时仍调用 Qwen | 只有 1 个候选时没必要让 Qwen 选择，且 Qwen 容易输出越界数字 | n=1 时直接返回唯一候选，跳过 Qwen |

修改文件：`llm_annotator.py`（`generate_with_text` 签名）、`region_selector.py`（`draw_candidates` + `qwen_select`）

### 已知限制

1. **Qwen 3B 标注质量有限**：对高度伪装图片识别率低，常用泛化描述（如 "bird" 而非精确描述）
2. **中文 prompt 问题**：3B 模型中文能力弱，已在代码中改为英文 prompt
3. **Step1 推荐 prescreen 模式**：OpenCV 做显著性检测 → 画编号框 → Qwen 只需命名，降低 3B 模型的任务难度

### 未来优化方向

1. **SAM3 Fine-tuning**：在 COD 数据集上微调 SAM3，改善伪装区域分割
2. **更强 Qwen 模型**：72B 模型标注质量显著高于 3B（代价：推理时间 x24）
3. **两阶段标注**：3B 粗标注 + 72B 精炼（成本仅为 72B 的 1/10）
4. **先验质量提升**：优化 SAM3 区域选择策略，减少错误先验
5. **负样本利用**：先验为"无人目标"时用黑图训练，显式学习"无目标时输出全黑"

---

## 九、环境配置

### 服务器端（cx1/cx2）

```bash
pip install torch torchvision  # CUDA 12.8
pip install transformers qwen-vl-utils accelerate
pip install opencv-python-headless pillow numpy
```

### 本地（Windows RTX 4070）

```bash
pip install torch torchvision  # CUDA 11.8
pip install opencv-python pillow numpy
```

### 验证

```bash
python verify_pipeline.py
# 1) 合并训练集加载（期望 4038+）
# 2) 测试集加载
# 3) 有 GPU 时跑 2 步 7 通道网络前向+反向