# 训练权重获取说明文档

## 概述

本项目的模型权重有两种获取方式：

| 方式 | 方法 | 耗时 | 效果 |
|:---|:---|:---|:---|
| **A. 下载已训练好的权重** | 从百度网盘下载 `.pth` 文件 | ~5 分钟 | 论文报告的完整实验结果 |
| **B. 从零训练** | 使用 `MyTrain.py` 本地训练 | ~3-8 小时 | 取决于数据量、硬件和超参数 |

---

## 方式 A：下载已训练好的权重（推荐）

### 链接
> **百度网盘**: https://pan.baidu.com/s/1nz_E-tiGLIE8u1NN3rixJA
> **提取码**: `mgc1`

### 操作步骤
1. 打开链接，输入提取码
2. 下载 `.pth` 文件
3. 放入 `model_pth/OUR/` 目录

### 优点
- ✅ **即拿即用**，无需等待训练
- ✅ **结果可复现**，与论文报告的指标完全一致
- ✅ **省电省硬件**，不需要 GPU 长时间运转
- ✅ **包含完整 4 个数据集**（COD10K/CAMO/NC4K/CHAMELEON）的评测结果

### 缺点
- ❌ 需要百度网盘账号（中国大陆用户较方便）
- ❌ 百度网盘非会员下载速度较慢（约 100-200 KB/s）

---

## 方式 B：从零训练

### 执行命令
```bash
python MyTrain.py --epoch 50 --batchsize 8 --trainsize 352
```

### 优点
- ✅ **不依赖外部下载**，有 GPU 即可开始
- ✅ **可自定义修改**网络结构、超参数、数据增强策略
- ✅ **可观测训练过程**，了解 loss 变化曲线和验证指标
- ✅ 骨干网络预训练权重（PVTv2-B2, 101MB）已本地就绪

### 缺点
- ❌ **耗时长**：50 个 epoch × 多尺度训练，约需 3-8 小时
- ❌ **功耗高**：GPU 满负荷运行耗电且发热
- ❌ **指标可能略低于下载的权重**，原因如下：

---

## 两种方式的性能差异分析

### 1️⃣ 数据量差异

| 训练数据 | 本机（CAMO-only） | 完整训练（论文） |
|:---|:---:|:---:|
| 训练图像数 | **1,000 张** | **~5,500 张** |
| 数据集 | 仅 CAMO | COD10K + CAMO + CHAMELEON + NC4K |
| 多样性 | 低（单一场景） | 高（4 种数据集混合） |

> **影响**：数据量越小，模型泛化能力越差。只使用 CAMO 训练得到的模型在 CAMO 测试集上可能尚可，但在其他数据集（COD10K/NC4K/CHAMELEON）上表现会显著下降。

### 2️⃣ 训练配置差异

| 配置项 | 本机训练 | 下载的权重 |
|:---|:---:|:---:|
| 训练轮数 | 50 epochs | 论文完整训练 |
| 学习率调度 | CosineAnnealing | 与论文一致 |
| 损失函数 | Hybrid E-loss | Hybrid E-loss |
| 网络结构 | PVTv2-B2 + CBAM | 完全相同 |
| 骨干权重 | ImageNet 预训练（pvt_v2_b2.pth）| 相同 |

> **影响**：网络结构完全相同，但 50 个 epoch 可能不足以完全收敛。如果时间允许，建议训练 80-100 个 epoch。

### 3️⃣ 预期指标对比（CAMO 测试集）

| 指标 | 下载的权重（预期） | 本机训练 50 epoch（预估） |
|:---|:---:|:---:|
| MAE ↓ | ~0.06-0.07 | ~0.08-0.12 |
| S-measure ↑ | ~0.85-0.87 | ~0.78-0.83 |
| E-measure ↑ | ~0.90-0.92 | ~0.85-0.88 |

> ⚠️ 以上为经验估算，实际结果取决于随机种子和训练过程。

---

## 决策建议

### 建议下载权重的情况
- 你只是想**运行推理/测试**，看看模型效果
- 你想运行 **GUI 图形界面**进行演示
- 你**没有 GPU** 或 GPU 显存低于 6GB
- 你需要**与论文一致的结果**进行对比

### 建议从零训练的情况
- 你无法访问百度网盘
- 你想**修改/改进**网络结构
- 你想**学习训练流程**，观察 loss 变化
- 你有**充足的时间**（> 4 小时）和 GPU

---

## 训练后的文件

无论哪种方式，最终你都会在 `model_pth/OUR/` 目录下得到类似的文件：

```
model_pth/OUR/
├── KKK_best_47.pth    # 最佳模型（MAE 最低的 epoch）
├── Net_epoch_10.pth    # 第 10 轮存档
├── Net_epoch_20.pth    # 第 20 轮存档
├── Net_epoch_30.pth    # 第 30 轮存档
├── Net_epoch_40.pth    # 第 40 轮存档
├── Net_epoch_50.pth    # 第 50 轮存档
└── log.log             # 训练日志
```

### 使用训练好的模型测试
```bash
python MyTest.py --pth_path ./model_pth/OUR/KKK_best_47.pth --gpu_id 0
```
