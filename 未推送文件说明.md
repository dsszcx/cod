# 📄 无法上传至 GitHub 的文件说明

> 本文档说明本项目中**无法推送到 GitHub 的文件/目录**及其原因，供备份与复现使用。
> 更新时间：2026-08-13

---

## 一、GitHub 推送限制说明

GitHub 对单个仓库的限制：
- **单文件大小限制：100 MB**（超过会被拒绝推送）
- 仓库总量建议 < 1 GB（超大仓库性能差）

因此，大体积的模型权重、数据集等**只能保存在本地**，不能上传。

---

## 二、无法上传的文件清单

### 1. SAM3 模型权重 `sam3.pt`

| 项目 | 说明 |
|:---|:---|
| **位置** | `sam3-main/sam3-main/checkpoints/sam3.pt` |
| **大小** | 3.2 GB |
| **原因** | 远超 GitHub 100MB 限制 |
| **来源** | HuggingFace `facebook/sam3`（需申请访问权限后下载） |
| **用途** | SAM3 文本 prompt 分割（新方案核心模型） |
| **获取方式** | 从 HuggingFace 重新下载，或本地备份 |

### 2. 数据集 `Dataset/`

| 项目 | 说明 |
|:---|:---|
| **位置** | `Dataset/`（TrainValDataset + TestDataset/CAMO） |
| **大小** | 约 200 MB（1250 张图 + 掩码） |
| **原因** | 数据版权 + 体积大，不宜公开 |
| **用途** | 训练/测试数据（CAMO 数据集） |
| **获取方式** | 从 CAMO 数据集官方渠道获取 |

### 3. 训练好的模型权重 `model_pth/`

| 项目 | 说明 |
|:---|:---|
| **位置** | `model_pth/OUR/`、`model_pth/PRIOR/` |
| **大小** | 每个 .pth 约 110 MB |
| **原因** | 权重文件体积大（>100MB），且为训练产物 |
| **用途** | 基础模型 + 先验引导模型的训练结果 |
| **获取方式** | 本地训练产生（约 1-2 小时/模型） |

### 4. LLM 先验标注数据 `outputs/`、`prior/`

| 项目 | 说明 |
|:---|:---|
| **位置** | `outputs/prior_annotations.json`、`prior/*.npz` |
| **大小** | JSON ~0.3MB + npz 1250 个 |
| **原因** | 服务器 LLM 标注产物，非代码 |
| **用途** | 先验引导训练的数据 |
| **获取方式** | 服务器 Qwen 标注产生 |

### 5. 推理结果 `results/`、`results_prior/`

| 项目 | 说明 |
|:---|:---|
| **位置** | `results/`、`results_prior/` |
| **大小** | 数百张预测图 |
| **原因** | 图片输出，非代码 |
| **用途** | 模型分割结果可视化 |
| **获取方式** | 本地推理产生 |

### 6. 虚拟环境 `.venv/`

| 项目 | 说明 |
|:---|:---|
| **位置** | `.venv/` |
| **大小** | 数 GB |
| **原因** | 本地 Python 环境，不可移植 |
| **用途** | 项目运行环境 |
| **获取方式** | `pip install -r requirements.txt` 重建 |

---

## 三、GitHub 上已推送的内容（供对比）

| 类别 | 内容 |
|:---|:---|
| 项目代码 | Network_PVTv2.py / Network_Prior.py / MyTrain / MyTest / dataloader / utils 等 |
| LLM 先验代码 | llm_annotator.py / prior_encoder.py / online_inference.py |
| 网页演示 | web_server.py + templates/index.html |
| SAM3 源码 | sam3-main/（**不含权重**） |
| 文档 | README（已改名 来源.md）/ 问题日志 / 论文方法论 等 |

---

## 四、如何恢复完整环境（需要大文件时）

```bash
# 1. 恢复 Python 环境
pip install -r requirements.txt

# 2. 恢复基础模型（重新训练约 1-2 小时）
python MyTrain.py

# 3. 恢复 SAM3 权重
# 从 HuggingFace facebook/sam3 下载 sam3.pt 放到 sam3-main/sam3-main/checkpoints/

# 4. 恢复 LLM 先验
# 服务器跑 llm_annotator.py 生成 prior_annotations.json

# 5. 恢复数据集
# 从 CAMO 官方渠道下载，按 dataloader 要求组织 Imgs/GT
```

---

> 以上文件均通过 `.gitignore` 正确忽略，不会误推送到 GitHub。